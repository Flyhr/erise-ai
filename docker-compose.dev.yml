# 废弃说明：
# 本文件对应的“开发环境专用 Compose 配置”已废弃。
# 现在统一使用仓库根目录下的 `docker-compose.yml + .env` 作为唯一官方入口。
#
# 如需启动系统，请使用：
#   docker compose --env-file .env -f docker-compose.yml up -d
#
# 保留本文件仅用于历史追溯，避免外部脚本因文件缺失直接报错。
services: {}
