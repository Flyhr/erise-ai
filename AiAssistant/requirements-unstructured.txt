# Compatibility mirror of the primary parser dependency chain.
# `requirements.txt` is the source of truth because Unstructured now runs as
# the default primary parser in the extraction pipeline.
unstructured[docx,pdf]>=0.16,<1.0
