# 废弃说明：
# 历史开发态 Dockerfile 已废弃。
# 现在统一使用 `erise-ai-ui/Dockerfile` 作为唯一官方镜像构建入口。

FROM node:24-alpine

WORKDIR /workspace

COPY package.json package-lock.json* ./
RUN npm install

CMD ["/bin/sh", "/workspace/docker/dev-entrypoint.sh"]
RUN apk add --no-cache bash
